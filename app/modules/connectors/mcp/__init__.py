"""MCP server for connector tools.

Exposes connector definitions and tool execution so AI agents
can discover available connectors and invoke tool calls via MCP.

This is registered as a separate endpoint that agents can query.
"""
